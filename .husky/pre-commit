#!/bin/sh

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks is required for commits. Install it with: brew install gitleaks" >&2
  exit 1
fi

gitleaks git --staged --redact --no-banner --config .gitleaks.toml
npm run build
